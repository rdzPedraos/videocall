{
  "name": "backend",
  "version": "0.0.0",
  "description": "this is a streaming proyect backend",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --watch server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "peer": "^1.0.0",
    "socket.io": "^4.6.1"
  }
}
